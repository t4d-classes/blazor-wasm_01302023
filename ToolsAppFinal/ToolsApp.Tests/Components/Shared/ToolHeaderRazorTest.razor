@inherits TestContext

@code {

  [Fact]
  public void ToolHeaderComponentRendersCorrectly()
  {
    // Arrange

    // Act
    var cut = Render(@<ToolHeader />);

    // Assert
    cut.MarkupMatches(
      @<header>
        <h3>The Tool</h3>
      </header>);
  }
  
  [Fact]
  public void ColorToolHeaderComponentRendersCorrectly()
  {
    // Arrange

    // Act
    var cut = Render(@<ToolHeader HeaderText="Color Tool" />);

    // Assert
    cut.MarkupMatches(
      @<header>
        <h3>Color Tool</h3>
      </header>);
  }  

}